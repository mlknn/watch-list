export type Stock = {id:string;symbol:string;companyName:string;currency:string;exchange:string;addedAt:string;addedPrice:number;initialQuoteTime:string;currentPrice:number;quoteTime:string;checkedAt:string;quoteError:string|null};
export type Watchlist = {id:string;name:string;createdAt:string;stocks:Stock[];role:'admin'|'viewer';shareToken?:string|null};
export type AccountState = {version:number;updatedAt:string;user:{id:string;name:string;email:string;country:string|null;hasBilling:boolean};plan:{id:'free'|'pro';maxLists:number;maxStocks:number;pageSize:number;trialEndsAt:string|null;expired:boolean};watchlists:Watchlist[]};
export const quoteTime=(value:string)=>new Date(value).toLocaleString(undefined,{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'});
export const price=(value:number,currency:string)=>/^[A-Z]{3}$/.test(currency)?new Intl.NumberFormat(undefined,{style:'currency',currency,minimumFractionDigits:2,maximumFractionDigits:value<1?4:2}).format(value):`${value.toFixed(2)} ${currency}`;
