import {InvalidUrl} from '@/components/product/invalid-url';
export const metadata={title:'Invalid URL — StockWatchlist',robots:{index:false,follow:false}};
export default function UnknownPath(){return <InvalidUrl/>;}
