'use client';
import {useEffect,useState} from 'react';
import world from '@/lib/world-map.json';
type CommunityData={available:boolean;users:number;countries:{code:string;users:number}[]};
export function Community(){const [data,setData]=useState<CommunityData|null>(null),[error,setError]=useState(false);useEffect(()=>{let active=true;async function load(){try{const response=await fetch('/api/community');if(!response.ok)throw Error();const result=await response.json() as CommunityData;if(active){setData(result);setError(false);}}catch{if(active)setError(true);}}void load();const timer=setInterval(()=>{if(!document.hidden)void load();},60000);return()=>{active=false;clearInterval(timer);};},[]);return <section className="community-section"><div className="community-copy"><p className="eyebrow">CURIOSITY HAS NO BORDERS</p><h2>A world of<br/>different perspectives.</h2><p>Ideas travel. Follow the market with people who see it a little differently.</p><div className="community-number"><strong>{data?.available?data.users.toLocaleString():'—'}</strong><span>{data?.available?'unique verified members':error?'Community count temporarily unavailable':'Community count appears after launch'}</span></div><small>Countries appear when at least 3 members choose to share their location. No precise locations are collected.</small></div><div className="community-map"><svg viewBox="0 0 900 440" role="img" aria-label="Member locations by country"><title>Optional country-level member locations</title>{world.map((country,i)=>{const members=data?.countries.find(c=>c.code===country.code);return <g key={i}><path d={country.path||''} fill={members?'#a4b8f1':'#e4eaf4'} stroke="#f7f9fc" strokeWidth="1"><title>{country.name}{members?`: ${members.users} members`:''}</title></path>{members&&<circle cx={country.x} cy={country.y} r={4+Math.min(7,Math.log2(members.users))} fill="#365bd8" stroke="white" strokeWidth="2"><title>{country.name}: {members.users} members</title></circle>}</g>;})}</svg>{!data?.countries.length&&<p className="map-empty">Your community’s map will grow here.</p>}<span className="map-attribution">Map: Natural Earth</span></div></section>;}
