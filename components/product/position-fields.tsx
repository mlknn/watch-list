 'use client';
import {useT} from "@/components/product/language";
import {T} from '@/components/product/language';
import {useId,useState,useEffect,type RefObject} from 'react';
import {Input} from '@/components/ui/input';
export type PositionDraft={quantity:string;cost:string;date:string;notes:string};
export const emptyPosition=():PositionDraft=>({quantity:'10',cost:'',date:new Date().toLocaleDateString('en-CA'),notes:''});
export function PositionFields({value,onChange,editing=false,currency='USD',quantityRef}:{value:PositionDraft;onChange:(value:PositionDraft)=>void;editing?:boolean;currency?:string;quantityRef?:RefObject<HTMLInputElement|null>}){const t=useT();const id=useId();const [noteOpen,setNoteOpen]=useState(!!value.notes);useEffect(()=>{if(!value.notes)setNoteOpen(false);},[value.notes]);const update=(field:keyof PositionDraft,next:string)=>onChange({...value,[field]:next});return <div className="position-fields"><label htmlFor={id+'-quantity'}><T text="Shares"/><Input ref={quantityRef} id={id+'-quantity'} aria-label="Number of shares" type="number" min="0.000001" max="1000000000" step="any" required value={value.quantity} onChange={e=>update('quantity',e.target.value)}/></label><label htmlFor={id+'-cost'}>{t("Cost per share")} ({currency})<Input id={id+'-cost'} aria-label="Cost per share" type="number" min="0.000001" max="1000000000" step="any" required={editing} placeholder={t("Latest price")} value={value.cost} onChange={e=>update('cost',e.target.value)}/></label><label htmlFor={id+'-date'}><T text="Purchase date"/><Input id={id+'-date'} aria-label={t("Purchase date")} type="date" required min="1970-01-01" max={new Date().toLocaleDateString('en-CA')} value={value.date} onChange={e=>update('date',e.target.value)}/></label><button type="button" className="note-toggle" aria-expanded={noteOpen} onClick={()=>setNoteOpen(v=>!v)}>{noteOpen?t("Hide note"):t("Add note")}</button>{noteOpen&&<label className="position-note" htmlFor={id+'-notes'}><T text="Investment note (optional)"/><Input id={id+'-notes'} aria-label="Investment note" maxLength={500} placeholder={t("Why are you watching this company?")} value={value.notes} onChange={e=>update('notes',e.target.value)}/></label>}<p className="field-note position-note">{t("Enter shares and costs adjusted for past stock splits. ")}{editing?'':t("Leave cost blank to use the latest quote. ")}{t("US, Europe (EUR), Canada (CAD), and Turkey (TRY). A list stays in the currency of the first stock you add. Quantity and purchase details are fixed after saving.")}</p></div>;}
